# Alpha-ID 决策记录

> 已确认的产品、技术、策略决策。只保留结论，不保留被替代的方案。
> 当前 Phase 定义以 PLAN.md 为准。

---

## 一、品牌与命名

| 项目 | 结论 |
|------|------|
| 项目全称 | **Alpha-ID** |
| Python 包名 | `alpha-id` |
| CLI 命令 | `aid` |
| DID 方法 | `did:aid:` |
| 文档用语 | 统一用 **Alpha-ID**（非 AID） |

**一句话定义**：把散落在数字世界里的你捡起来——收集、重组、延续，让它替你活着、替你社交、替你学习。

---

## 二、产品定位（10条核心决策）

> **优先级声明原则**：
> - ✅ **核心定位**：必须做
> - ⏸ **当前聚焦**：当前阶段先做，其他方向暂缓
> - 🚫 **永久原则**：永远不碰的红线
> - ⚠️ **条件约束**：特定条件下适用

| # | 结论 | 优先级 | 触发条件（什么时候重新评估） |
|:-:|:-----|:------:|:------------------------|
| 1 | **核心定位：把散落的你捡起来** — 收集、重组、延续数字世界里的碎片 | ✅ 核心定位 | — |
| 2 | **当前阶段聚焦核心能力** — 先做好采集、重组、展示 | ⏸ 当前聚焦 | 核心价值验证完成 |
| 3 | **不做模型开发** — 换模型时 Alpha-ID 不换 | ⏸ 当前聚焦 | 出现比 LLM 更核心的推理能力 |
| 4 | **不做框架/SDK** — 面向最终用户 | ⏸ 当前聚焦 | 出现企业级接入需求 |
| 5 | **不做平台** — 寄生策略更轻 | ⏸ 当前聚焦 | 寄生策略无法持续获取用户 |
| 6 | **不造协议** — 搭便车：MCP + A2A | ⏸ 当前聚焦 | 行业标准缺失或不适用 |
| 7 | **DID 对用户无意义** — 藏在体验下面 | 🚫 永久原则 | — |
| 8 | **私钥永远在客户端** — 大脑只验证不签名 | 🚫 永久原则 | — |
| 9 | **core/ 零外部依赖** — 依赖向内 | 🚫 永久原则 | — |
| 10 | **I2I + A2A 两者都要** — I2I 是关系骨架，A2A 是行动血肉 | ✅ 核心定位 | — |

---

## 三、锁定策略

| 方向 | 策略 |
|------|------|
| 冷启动 | 导入已有数字痕迹 + 本机信号扫描 |
| 导入器顺序 | ChatGPT → Claude → GitHub → 浏览器 |
| 魔法时刻 | 导入后回到原工具 → 发现 Agent 已认识自己 |
| 推广 | 推现象不推产品 |
| 商业模式 | AID 存在 = 永远免费 |
| Computer Use | 像人一样看屏幕+操作，不写平台 API |

---

## 四、核心架构

```
 ┌──────────────────────────────────────┐
 │              用户客户端                │
 │  身份 | 隐私 | 记忆 | 社交 | 安全      │
 │  私钥永远在客户端                      │
 └──────────────────┬───────────────────┘
                    │ API
 ┌──────────────────┴───────────────────┐
 │           TwinBrain（大脑）            │
 │  记忆整理 | 社交行为 | 状态管理        │
 │  只能响应/执行 | 不能伪造身份          │
 └──────────────────┬───────────────────┘
                    │
 ┌──────────────────┴───────────────────┐
 │              存储层                    │
 │   结构化数据 | 向量记忆                 │
 └──────────────────────────────────────┘
```

核心原则：私钥在客户端 | 记忆是用户资产核心

---

## 五、Ghost Layer 与双大脑

| 维度 | 定位 |
|------|------|
| 本质 | 坐在所有 AI 工具之上的幽灵层 |
| 做什么 | 不造新 AI，让已有 AI 认识你 |
| 对用户价值 | 你打开任何 AI，它都认得你 |
| 存在方式 | 后台工作，用户几乎不主动打开 |

| 大脑 | 职责 |
|------|------|
| **内在大脑** | DID + Profile 画像 → 定义"你是谁"——不变的身份锚点 |
| **外在大脑** | 三层记忆 + 因果图谱 + 关系图谱 → 记录经历、决策、关系 |

**I2I + A2A**：没有 A2A，关系是空架子。没有 I2I，Agent 协作不知道替谁干活。两者缺一不可。

---

## 六、外置大脑三层架构

```
┌──────────────────────────────────────┐
│  第三层：大模型适配层                   │
│  身份设定 + 框架约束 + 表达风格 + 记忆注入 │
├──────────────────────────────────────┤
│  第二层：思维框架引擎（核心壁垒）        │
│  宇宙星链→第一性原理→反向推翻           │
│  →递归降维→反脆弱→节律控制→全息追溯     │
├──────────────────────────────────────┤
│  第一层：用户画像大脑                   │
│  DID + 画像 + 三层记忆 + 因果图谱       │
└──────────────────────────────────────┘
```

**五个底层错误的应对**：
1. AI Agent 无"自我" → DID + 本地私钥
2. MCP/A2A 没解决"谁在调用" → MCP 身份层注入
3. LLM 推理是伪因果 → 因果图谱 + 七框架约束
4. Agent 安全是事后补丁 → PoE 执行证明 + DID 签名审计链
5. 个人 AI 都是中心化黑盒 → 本地优先 + 私钥自持

**核心原则**：每次纠结"要不要加这个功能"，问——**它让 AI 更懂"为什么我这么想"了吗？** 是就加，不是就删。

---

## 七、P0 范围冻结（2026-06-05，唯一短期目标）

| 决策 | 结论 |
|:-----|:-----|
| **P0 唯一目标** | `aid collect chatgpt <zip>` → 解析 → 展示文本画像 → 说"有了" |
| **P0 不做** | MCP注入 / HTML粒子 / Safety / A2A / 语音 / 多源采集器 |
| **死线** | PyPI v0.1.0 发布 |
| **模型默认值** | `deepseek-v4-flash`，环境变量 `LLM_MODEL` 覆盖 |

---

## 八、代码约束

- Python 3.12+, pytest, ruff
- CLI: Typer，入口 `aid = "alpha_id.cli:app"`
- 核心逻辑在 `core/`，不依赖外部包
- ⏸ 当前聚焦：不改已有测试（除非发现 bug）
- ⏸ 当前聚焦：不引入 LangChain（避免复杂度）

---

## 九、Phase 路线图

| Phase | 内容 | 状态 |
|:------|:-----|:-----|
| Phase 0 | 画像骨架 | ✅ |
| Phase 1 | 多源采集 + 零数据冷启动 + 场景识别 | 🔄 |
| Phase 2 | MCP 注入 + 思维框架引擎（3框架+A/B测试） | ⏳ |
| Phase 3 | I2I 关系网络 + 完整七框架 + 数字遗嘱 | ⏳ |

---

## 十、语音交互方向

| 方向 | 做什么 | 阶段 |
|:-----|:-------|:-----|
| 语音输入（ASR） | 语音→文字→进入记忆系统 | Phase 1 |
| 语音输出（TTS） | Alpha-ID 通过语音回答、提醒、播报 | Phase 1 |
| 声线人格化 | 让 Alpha-ID 说话像用户 | Phase 2 |
| 唤醒词 | 用唤醒词激活 Alpha-ID | Phase 2 |

**技术路线**：ASR/TTS 用系统原生 API，不追求完美，先跑通通路。

---

> **维护原则**：每次加决策前，先问——这条如果删掉，项目会死吗？如果不会，就不要加。
