# Alpha-ID 决策记录

> 本文件记录所有已确认的产品、技术、策略决策。
> 每次新增决策时追加到此文件，不再重复讨论。
> 创建日期: 2026-05-30

---

## 一、品牌与命名

| 决策 | 结论 | 确认来源 |
|------|------|---------|
| **项目全称** | **Alpha-ID** | ki.md 全文贯通 |
| Python 包名 | `alpha-id` | pyproject.toml，无需更改 |
| 源码目录 | `src/alpha_id/` | 当前状态，无需更改 |
| CLI 命令 | `aid`（缩写） | pyproject.toml，与 DID 一致性，无需更改 |
| DID 方法 | `did:aid:` | 已实现，无需更改 |
| 文档/宣传用语 | 统一用 **Alpha-ID**（非 AID）| ki.md 全文贯通 |

→ **结论：命名无需更换。** 当前代码库命名正确。唯一需做的是文档中混用的 "AID" 纠正为 "Alpha-ID"。

---

## 二、产品定位（不再讨论）

| # | 结论 | 一句话 |
|:-:|------|--------|
| 1 | **Alpha-ID 是数字灵魂 / Ghost Layer**，不是 App，不是 SDK | 坐在所有 AI 工具上面的那一层 |
| 2 | **不做模型** | Alpha-ID 坐在所有模型上面，换模型时 Alpha-ID 不换 |
| 3 | **不做框架/SDK 开发者工具** | Alpha-ID 是产品，面向最终用户 |
| 4 | **不做平台**（区别于 MiMo） | 做去中心化身份层，不做中心化分发平台 |
| 5 | **不造协议，搭便车** | MCP（对外暴露层）+ A2A（社交层） |
| 6 | **不做插件做的事** | 做插件做不了的事：跨平台操作、自主决策、统一上下文 |
| 7 | **DID 对最终用户无意义** | 必须藏在体验下面，不暴露技术术语 |
| 8 | **私钥不在大脑里** | 大脑只做验证不做签名。签名必须用户客户端私钥完成 |
| 9 | **Agent = 简单的 LLM + Tools + Loop** | 不要复杂框架（LangChain/CrewAI） |
| 10 | **依赖向内** | `src/core/` 层零外部依赖，可嵌入任何框架 |

---

## 三、锁定策略（不再推导）

| 方向 | 策略 |
|------|------|
| **冷启动** | 不从零认识用户 → 导入已有数字痕迹（ChatGPT/GitHub/Claude/浏览器） |
| **导入器顺序** | ChatGPT → Claude → GitHub → 浏览器书签+历史 |
| **"魔法时刻"** | 导入后回到原工具 → 发现 Agent 已经认识自己 → 制造传播现象 |
| **产品登场** | 蒙面英雄: 先以"导入工具"普通人形态出现，再用魔法时刻震撼 |
| **推广** | 推现象不推产品（"ChatGPT 突然认识我了"） |
| **商业模式** | Alpha-ID 存在免费 / 记忆仓库 $4.99/月或$199买断 / 生态抽成5% / 硬件Key $79 |
| **MiMo 关系** | 借用其分发层（npx 安装），我们做身份+信誉层，不竞争 |
| **Computer Use** | 已有 Skill 层（screen_capture/window_control/OCR）→ 补 Plan 和 Safety 层——不必为任何平台写API，像人一样看屏幕+操作 |
| **BCI 原则** | 今天为明天设计：意图驱动、异步执行、默认信任、持续在线 |

---

## 四、核心架构（来自原始设计图）

```
 ┌──────────────────────────────────────────────────┐
 │                  用户客户端                        │
 │   身份(Identity) │ 隐私(Privacy) │ 记忆(Memory)   │
 │   社交(Social)   │ 安全(Security)                  │
 │   私钥永远在客户端 │ 只能做签名/申请 │ 不能伪造身份  │
 └──────────────────────┬───────────────────────────┘
                        │ API
 ┌──────────────────────┴───────────────────────────┐
 │                   TwinBrain（大脑/云端）           │
 │   记忆整理 │ 社交行为 │ 状态管理                    │
 │   自主学习 │ 安全审计                              │
 │   只能响应/执行 │ 不能伪造身份                      │
 └──────────────────────┬───────────────────────────┘
                        │
 ┌──────────────────────┴───────────────────────────┐
 │                   存储层                           │
 │        Postgres（结构化数据）                      │
 │        Vector DB（记忆向量）                       │
 └──────────────────────────────────────────────────┘

 核心原则：
 - 私钥在客户端，大脑只有验证能力
 - 日志/行为/关系/状态全在服务端
 - 记忆是用户资产的核心
```

---

## 五、语音交互（已确认）

| 维度 | 决策 | 来源 |
|------|------|------|
| **语音方向** | **双向交互**（语音输入 + 语音输出）| 2026-05-30 用户确认 |
| **输入方式** | 麦克风 → ASR 转文字 → 送入 TwinBrain | 同上 |
| **输出方式** | Agent 回答 → TTS 播报（可选，默认文本） | 同上 |
| **声线声型** | **Phase 2 再定**，先建立语音通路，人格化声音是后续工作 | 同上 |
| **架构位置** | 作为 Tool 层：`voice_input_tool` → 转录后走标准 Message 通道 | 同上 |

---

## 六、阶段优先级

| 优先级 | 阶段 | 内容 | 评分影响 |
|:------:|:----:|------|:--------:|
| **P0** | 基础设施 | 遗留代码清理（删除 src/agents/ 和 src/graphs/）+ 文档名称统一 | 不变 |
| **P1** | Phase 0a | 语音输入 Tool + 导入器架构 | 4.0→4.1 |
| **P1** | Phase 0b | ChatGPT/GitHub 导入 + 画像联动 | 4.1→4.2 |
| **P2** | Phase 1 | MCP Server + Daemon 进程 | 4.2→4.4 |
| **P3** | Phase 2 | Computer Use 闭环 + 声线 TTS | 4.4→4.5 |
| **P4** | Phase 3 | 自主生存 + 晨报系统 | 4.5→4.7 |

---

## 七、代码约束

- Python 3.12+, 类型注解全覆盖, pyright basic
- 测试: pytest, 每次修改后跑 `python -m pytest tests/ -q`
- 格式化: ruff（配置在 ruff.toml）
- CLI 入口: pyproject.toml 中 `aid = "alpha_id.cli:app"`, 用 typer
- 新增模块必须加 `__init__.py`, 导出公共接口
- 核心逻辑写在 `core/`, 不依赖外部包
- ❌ 禁止引入 LangChain 作为依赖
- ❌ 禁止修改已有测试文件（除非只改预期值）
- ❌ 禁止发 git commit / 建分支
- ❌ 禁止加版权头
- ❌ 禁止改与当前任务无关的代码
